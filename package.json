{
  "name": "ai-agent-demo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"cd kris-agent-server && mvn spring-boot:run\" \"npm run dev --prefix web\"",
    "dev:web": "npm run dev --prefix web",
    "dev:server": "cd kris-agent-server && mvn spring-boot:run"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
